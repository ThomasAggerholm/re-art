{% assign s = section.settings %}
{% assign nav = s.nav %}

<ul class="{{ class }}__menu">
{% for link in linklists[nav].links %}
    <li class="menu__item"><a href="{{ link.url }}" class="menu__item__link">{{ link.title }}</a></li>
{% endfor %}
</ul>